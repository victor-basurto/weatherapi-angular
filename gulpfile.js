var gulp = require( 'gulp' ),
	nodemon = require( 'nodemon' ),
	sass = require( 'gulp-sass' ),
	autoPrefixer = require( 'gulp-autoprefixer' ),
	liverReload = require( 'live-reload' );
/**
 * TODO: Finish live reload,
 * 		 setup gulp
 */

/**
 * change css files
 */
//gulp.task( 'app-styles', function() {

//})